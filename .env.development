# Vite dev (`npm run dev`) — local backend (LegendPipsBackend on port 5000)
VITE_API_BASE_URL=http://localhost:5000/api

# Production API (only if testing against live server — KYC routes must be deployed there):
# VITE_API_BASE_URL=https://legendpips.com/api
