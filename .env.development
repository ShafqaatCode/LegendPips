# Vite dev (`npm run dev`) — change this if you run the backend locally
VITE_API_BASE_URL=https://legendpips.com/api

# Local backend (LegendPipsBackend on port 5000):
# VITE_API_BASE_URL=http://localhost:5000/api
